In Backend Developer 2 major component:
1. Programing language 2. Database (ORM - object relational mapping, ODM- Object document mapper)
Files directory:
src - index(DB connect), APP (configuration, cookie, urlencode), Constants(enums, DB name)
1. DB Folder()
2. Models folder (Database schema)
3. Controllers(Functionality)
4. Routes
5. Middlewares
6. Utils (repeated function)


-----------------------------------
Why we use nodemon?
import - module express -  meaning 
require - commonJs - means code get loaded synchronously.
For the above two, we use "type" in package.JSON
CORS error -  cross origin error
Proxy is a solution for CORS error
Middleware

